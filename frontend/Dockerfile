FROM node:18-alpine

WORKDIR /app

# Copier les fichiers package
COPY package*.json ./

# Installer les dépendances
RUN npm install

# Copier le code source
COPY . .

# Exposer le port
EXPOSE 3000

# Démarrer avec Vite en mode dev (hot reload)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
